import 'package:flutter/material.dart';

class SavedCities extends StatelessWidget {
  const SavedCities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("SavedCities");
  }
}
